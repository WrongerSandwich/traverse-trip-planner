# Build artifacts
node_modules
build
.svelte-kit

# Secrets (env file is provided at runtime via docker-compose env_file)
.env
.env.*

# Everything user-managed (trip stages, home.md, settings.json, .cache/) lives
# under data/ post-#411 and is provided at runtime via the bind mount.
data

# Legacy pre-#411 paths — guard against stragglers at the repo root.
ideas
planning
completed
archived
home.md
settings.json
.cache
.geocode-cache.json
.image-cache.json
.route-cache.json
.workflow-stats.json

# Tooling, tests, docs — not needed in the runtime image
.claude
.git
.gitignore
.github
tests
docs
*.log
/tmp
.DS_Store

# This file itself
.dockerignore
